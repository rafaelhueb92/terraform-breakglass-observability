# Terraform working files and local state
**/.terraform/*
*.tfstate
*.tfstate.*
crash.log
crash.*.log
*.tfvars
*.tfvars.json
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Lambda build output and deployment archive
modules/ingestion/converter.zip
modules/ingestion/converter-dependencies-layer.zip
modules/ingestion/build/
lambda/converter/*
!lambda/converter/handler.py
!lambda/converter/requirements.txt

# Local AWS credentials
credentials
*.backup
.terraform.lock.hcl
